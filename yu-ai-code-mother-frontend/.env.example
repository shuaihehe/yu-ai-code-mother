# 后端接口地址。开发环境建议使用 /api，通过 Vite 代理到 8123 端口。
VITE_API_BASE_URL=/api

# 已部署应用的访问域名，需要与后端 AppConstant.CODE_DEPLOY_HOST 保持一致。
VITE_DEPLOY_DOMAIN=http://180.76.97.198

# 当前后端尚未实现以下接口，补齐后改为 true 即可启用对应前端功能。
VITE_ENABLE_CHAT_HISTORY=false
VITE_ENABLE_CODE_DOWNLOAD=false
